{
  "name": "cablescope",
  "version": "1.0.0",
  "description": "USB-C Cable & Port Inspector — Windows tested, macOS/Linux untested",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "clean": "node -e \"const fs=require('fs');['node_modules','dist','src-tauri/target','src-tauri/gen'].forEach(d=>fs.rmSync(d,{recursive:true,force:true}))\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "sharp": "^0.35.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
